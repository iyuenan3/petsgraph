# RELATIONS：petsgraph

## 出向依赖（我用了谁）

当前没有运行时项目依赖。李五百睡觉陪伴 MVP 离线运行，不依赖账号、服务端、数据库、分析平台或网络 API。

| 依赖 | 用途 | 边界 |
|---|---|---|
| 用户自行配置的 Seedance 等视频生成服务 | 生成动作视频母片 | 只属于素材制作侧，不进入桌面运行时；凭据不写入仓库或宠物包 |
| 本地逐帧抠图与包编译工具 | 抠图、归一化、命中区、root motion、校验与打包 | 由未来素材 Skill 编排，输出数据包，不向运行时暴露生成实现 |

## 只读参考项目

- `mochi-master` 只读参考快照：参考透明桌面窗口、内容寻址素材、生成服务适配和逐帧抠图。它不是代码依赖，petsgraph 不继承其平面动作表、固定时长切换或动态 WebP 唯一运行时路线。
- `petsdesk` 前任项目：只用于复盘窗口交互、真实窗口位移、素材验收和失败路线。petsgraph 不复用其九宠产品边界、持续活跃调度、随机行为评分或速度标量动画契约。

## 入向（谁用我）

- 当前 petsgraph Swift/AppKit 桌面运行时消费 `.petsgraph-pet` 预览包。
- 未来 petsgraph 素材生成 Skill 产出符合 `SPEC.md` 的素材包。
- 当前没有其他项目把 petsgraph 作为依赖。

## 共享底座 / 复用资产

暂无共享基础设施或跨项目公共库。若未来抽出通用包校验器或图集编译器，先确定属主项目，再在此处只保留指针。
