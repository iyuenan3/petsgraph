# ROADMAP：petsgraph

## Now（当前焦点）

完成 Phase 0 单宠动作图连通闭环：先完成新增左侧伸展与仰卧闭环的真实桌面验收，再把已经素材级批准的右向移动、正面坐姿、坐趴双向桥接、趴卧、香箱和其他睡姿素材编入同一坐标契约，并证明新增起步边的 root motion、真实原生窗口移动和自然边缘停车。

闸口：右向整链达到 `runtime-chain-approved`。在此之前不批量生成完整动作包。

当前进度：五百已有 9 个循环动作与 18 条有向边完成人工素材验收并固化生产履历。右向站走跑素材链、正面坐姿循环、站到坐、坐到右向行走、坐姿与趴卧双向桥接，以及多组趴卧、香箱、左侧蜷卧、左侧伸展和仰卧闭环均已通过素材验收。用户已接受 30 pt/s 的走路窗口速度，跑步保持 115 pt/s；趴卧与左侧蜷卧子图已通过真实桌面运行时链。0.2.0 聚焦预览包已接入左侧伸展与仰卧双向闭环并完成正常速度和慢放录制，仍等待 Maxwell 的运行时结论。坐趴桥接和其余批准素材尚未编入同一完整包，坐到右向行走的窗口位移曲线也未验收，完整包仍不可安装。

## Next

1. 取得 `rest.side-stretched.left ↔ rest.supine.left` 聚焦预览链的真实桌面人工结论，未通过则只修正该链，不扩大编译范围。
2. 把正面坐姿、`sit.front ↔ rest.prone.left`、香箱和其他已批准睡姿编入新的带版本号完整包，验证所有配方哈希、帧序、固定变换和图引用。
3. 验收 `sit.front → gait.walk.right` 的 root motion、完整连通运行时链与自然边缘停车，达到整包 `runtime-chain-approved` 后再允许安装。
4. 以独立左向素材复现同一移动闭环，验证左右真实特征与方向一致性。
5. 补齐右侧躺姿及其双向过渡，再加入玩耍、进食和舔毛有限活动。
6. 完成 30 分钟行为调度观察，并把已验证的生成、抠图、标注、验收和编译流程抽象为可暂停的素材生成 Skill。
7. 完成严格包导入、本地版本切换、失败回滚与正式单宠安装体验。

## Later

- 从 PNG 序列编译分页图集，并以真实性能数据决定是否作为正式运行时格式。
- 增加更多稳定姿态、活动变体、低频注视与休息行为。
- 用第二只高辨识度宠物验证素材 Skill 的通用性。
- 评估跨显示器移动、更多 macOS 系统场景和长期功耗。
- 第一阶段稳定后再讨论其他桌面平台。

## 已搁置（+原因）

- 多宠互动：会把关系、碰撞、遮挡和素材规模同时引入，偏离单宠质量主线。
- 云端账号、同步、市场与付费代生成：用户自行 clone 并配置 provider，项目不承接生成服务成本与私密素材托管。
- 骨骼、肢体拼接、Hatch Pet、RIFE、光流和自动补间作为主路线：既有参考项目已证明机械通过不等于真实动作自然。
- 运行时只播放动态 WebP：无法保证精确逐帧寻址、安全退出和 root motion 同步。
- 桌面二维自由漫游与跨显示器：第一阶段只验证固定 y 的水平活动带，降低窗口运动与边界复杂度。
