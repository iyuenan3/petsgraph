# PetsGraph 下一代重构验收记录

记录时间：2026-08-23T04:02:07+08:00

最终审查更新：2026-08-23T05:48:43+08:00

内容基线提交：`6d1a4b5`

最终审查代码提交：`bbe8997`

结论：本地目录迁移、公开与私有边界、PetPack 1.0、五百与飞流候选转换、双平台 Player 机械实现、自动化验证、远端 push 与三条 GitHub Actions 工作流已经完成。整个重构 Goal 仍未完成，因为 Apple Silicon macOS 与 Windows x64 真实 GUI 及同包正常速度视觉验收尚未通过。本记录不得作为 `1.0.0` 发布批准。

## 验收原则

- `已证明` 只表示下表列出的证据直接覆盖该要求。
- `机械完成` 表示代码、测试、构建或包结构已经验证，但不能替代真实桌面观看。
- `待外部验收` 表示需要 GitHub、真实 Windows 设备或用户视觉判断。
- 所有路径均相对项目根；被 Git 忽略的私有路径只作为本机审计指针，不进入公开 clone。

## 逐项结论

| # | 要求 | 状态 | 当前证据 | 剩余门禁 |
|---:|---|---|---|---|
| 1 | 文档、目录契约与迁移规则先行，区分 Target 与 As-built | 已证明 | `86e121d`、`946c22e` 先冻结产品与迁移契约，README 与 AIREADME 当前均区分历史发布、机械实现和人工验收；提交已经推送并完成远端回读 | 无 |
| 2 | 清理失败、重复、缓存与临时产物，保留审计和恢复能力 | 已证明 | `pets/audit/cleanup/petsgraph-cleanup-2026-08-23-v1.md`；89 个文件、36,878,039 bytes 位于系统回收站；旧根 Swift 缓存 2,991 个文件、304,741,588 bytes 也位于回收站 | 用户可自行复查回收站，本轮没有永久删除这些历史文件 |
| 3 | 收敛为职责明确的唯一项目根 | 已证明 | 根现有 `player/`、`petpack/`、`studio/`、`pets/`、`petpacks/`、`codexpets/`、`assets/brand/`、`docs/`、`release/`、`.local/`；旧根 `workspaces/`、`output/`、`tmp/`、`dist/`、`.cache/`、`.venv*` 均不存在 | 无 |
| 4 | 公开 Git 与私有制作边界 | 已证明 | `git ls-files` 不包含 `studio/`、`pets/`、`petpacks/`、`.local/` 或 `.env.local`；`studio/` 没有嵌套 Git；Player 没有 `.petpack`、`Pets/` 或真实宠物素材；远端双平台零素材构建通过 | 持续由公开 CI 防回归 |
| 5 | Studio、provider 与环境变量一次性迁移 | 已证明 | `studio/.env.example` 记录两把 GPT Image、两把 Seedance、一把 Seedream 和一把 Seedance 加 Seedream Key；代码使用新名称，旧 `ARK_API_KEY`、`ARK_API_KEY_BAK` 只触发迁移错误；真实 `.env.local` 未被读取或跟踪 | 无 |
| 6 | `codexpets/` 迁移且保持公开包身份和字节 | 已证明 | `76ad2ea` 与 `47608a0`；公开清单仍为 `wubai-v0` 与 `feiliu-hatch-native-v1`；校验器及 3 项安装回归通过，四个媒体与描述文件摘要和字节数与迁移前记录一致，多宠安装失败会整体回滚 | 无 |
| 7 | PetPack 1.0 契约、schema、夹具与验证器 | 已证明 | 5 份 schema、参考验证器、基线和前向兼容夹具；33 项 Python 回归通过；每个 clip 恰好一个表示，显式目录、条目注释、强加密、数据间隙、数据描述符、越界整数和歧义版本均会被拒绝；基线 SHA-256 `812f0459fe444ff4cf657908d3c9b235be21f591d796ac7d0f02e50f564ac2c1`，前向兼容 SHA-256 `d0f5273cbf930e2ddd12865a62311d0d2058c4a1749b07b33448d84411ca08dc` | 无 |
| 8 | 五百、飞流使用批准媒体转换，移除交互与窗口位移 | 机械完成 | 五百候选 36 clip、12 节点、26 边，SHA-256 `14f719b67da95a4cf089500aedc7c67fb6c74c3a63a273052190856f99b3e0ef`；飞流候选 25 clip、9 节点、16 边，SHA-256 `f0308cd322fbbd1ef1259e64ca3f93a8f7da58b202aa441baef5a26fe61aef25`；旧新媒体不匹配数为 0；interaction 节点、关联 clip 和窗口 root motion 已排除 | 两包仍为 `mechanically-validated-awaiting-player-runtime-review`，待双平台正常速度人工观看 |
| 9 | Apple Silicon macOS 与 Windows x64 零素材多宠 Player | 机械完成 | Swift 21 项、MSTest 38 项通过；Windows 全解决方案 Release 构建 0 警告、0 错误；两个平台实现持久导入激活日志、失败与重启回滚、事务卸载、保守设置恢复、真实媒体摘要校验、预加载失败留在当前循环、固定位置、显示隐藏和统一大小；本机构建 2 已观察到合成宠物动画刷新 | 最终构建 3 的安装启动被 macOS 锁屏阻断；两个真实宠物、全部菜单、长时间多宠、透明命中、DPI 与视觉连续性仍待人工验收 |
| 10 | 保留 v0.6.0 回滚事实，只迁移可匹配位置与全局大小 | 已证明 | 本地与远端 `v0.6.0` 注解标签最终指向 `ce4570cef6f47fe75b32df40c1476b0657a1d999`；GitHub Release 两个附件仍与 `release/manifests/v0.6.0.json` 一致；`6d1a4b5` 修正双平台逻辑，不再迁移旧显示状态，并新增两项回归 | 真实升级路径仍需 GUI 验收，不能修改或覆盖旧 Release |
| 11 | 小葵本轮只迁移资料 | 已证明 | `pets/personal/xiaokui/` 当前 369 个文件；没有小葵 PetPack；批准母图、生成记录和评审资料只改变职责路径 | 后续新 Goal 才能继续生成、抠图和制包 |
| 12 | 小切片提交、及时 push、双平台同包一致性与完整证据 | 部分完成 | 可回滚小提交持续推送；最终审查修复分别为 `8b8f902`、`b417a6b`、`47608a0`、`bbe8997`；远端 `main` 已回读到 `bbe8997`；PetPack、macOS、Windows 新代码运行与两个历史 Release 只读复验全部成功；三个验证器覆盖同一合成包，两个原生实现还验证五百与飞流相同候选及相同 SHA-256 | 真实双平台同包观看和用户批准未完成 |

## 当前可复验结果

### 自动化测试

| 范围 | 结果 |
|---|---:|
| PetPack Python | 33 项通过 |
| macOS Swift | 21 项通过 |
| Windows MSTest | 38 项通过 |
| Codex 宠物 | 3 项通过 |
| Windows Release 解决方案 | 0 警告、0 错误 |
| 格式 | `swift-format lint` 与 `dotnet format --verify-no-changes` 通过 |

双平台原生加载器共同验证的五个包：

1. store 基线夹具，SHA-256 `812f0459fe444ff4cf657908d3c9b235be21f591d796ac7d0f02e50f564ac2c1`。
2. 前向兼容夹具，SHA-256 `d0f5273cbf930e2ddd12865a62311d0d2058c4a1749b07b33448d84411ca08dc`。
3. deflate 临时夹具，SHA-256 `611586d4c93669e45c89fd30a42ef358210bd468f1a271a3a3af22a240725fa8`。
4. 五百候选，SHA-256 `14f719b67da95a4cf089500aedc7c67fb6c74c3a63a273052190856f99b3e0ef`。
5. 飞流候选，SHA-256 `f0308cd322fbbd1ef1259e64ca3f93a8f7da58b202aa441baef5a26fe61aef25`。

### 本地开发产物

这些文件只用于下一步人工验收，不是 Release 附件：

- macOS：`/private/tmp/PetsGraph-0.7.0-review3.app`。版本 `0.7.0-review`、构建号 3，主程序为 `Mach-O 64-bit executable arm64`，ad-hoc 严格签名延迟复验通过，主程序 1,208,928 bytes，SHA-256 `ce6717898384ff42c9ba40175ebb7640aa83ce2b1d8946cd67e59659985a7520`。App 资源只有 `PetsGraph.icns`，没有 `.petpack`、`Resources/Pets` 或真实宠物素材。该最终构建尚未安装启动，因为 Mac 已锁屏，不能把构建证据写成人工 GUI 通过。
- Windows：`.local/dist/builds/PetsGraph-v0.7.0-dev-Windows-x64.zip`。文件 75,267,412 bytes，SHA-256 `6d90aa7cada380a30d8a348cac1a5e3c6806ca67ca08be7a135b31da036abac8`，压缩完整性通过，主程序为 `PE32+ executable (GUI) x86-64`，包内没有 `.petpack` 或 `Pets/`。

macOS 构建 2 已安装并用公开合成宠物启动。短间隔屏幕采样观察到两个不同画面摘要，证明渲染会刷新，但这只覆盖合成夹具的最小动画，不覆盖五百、飞流身份、菜单、拖动或正常速度视觉批准。

## 历史回滚证据

2026-08-23 只读回查 GitHub Release：

- `v0.6.0` 已发布，不是草稿或预发布。
- macOS 附件 `PetsGraph-v0.6.0-macOS-arm64.dmg` 为 857,640,594 bytes，SHA-256 `04c6f30e35d7dd8b5b096d0051aad628987d59b8245d84060cf704f02709b159`。
- Windows 附件 `PetsGraph-v0.6.0-Windows-x64.zip` 为 913,953,281 bytes，SHA-256 `90578d6620ef9c221c173b173c24631d6e756b372b532030f8669994d22b0015`。
- 本地可下载副本位于 `.local/dist/published/v0.6.0/`，GitHub Release、不可移动标签和公开 manifest 才是事实源。

## 人工验收门禁

### macOS

启动开发 App 前先备份现有 `~/Library/Application Support/PetsGraph/` 和相关 UserDefaults，因为开发版沿用正式应用身份并会写入新数据根。之后必须人工完成：

1. 零宠物启动，只保留状态栏入口。
2. 依次装载五百和飞流，确认菜单名称来自包，不写死。
3. 正常速度同时观看，确认独立时钟、无卡帧、无闪动、无硬切、无窗口位移。
4. 分别与全部执行显示、隐藏、卸载，确认隐藏跨重启保持，卸载只删除内部副本。
5. 拖动两只宠物并重启，确认位置保持；逐档检查 `0.5` 至 `2.0` 全局大小。
6. 检查透明区域点击穿透、宠物不响应点击动作、会议投屏隐藏语义和长时间内存表现。
7. 由用户明确记录正常速度视觉结论，未明确通过前不得批准候选包。

### Windows x64

在真实 Windows 11 x64 设备上解压开发 ZIP 后，必须人工完成与 macOS 相同的装载、多宠、显示隐藏、卸载、拖动、缩放、重启和正常速度视觉检查，并额外覆盖系统托盘、SmartScreen、DPI、多显示器和透明命中。真机结果必须记录 Windows 版本、缩放率、显示器配置、包 SHA-256 和明确结论。

## 远端验证

首次推送把远端 `main` 从 `cd11a52f8229a8366029ef0210a08fba17cc60e2` 前进到 `a7c2cb594734fa4e337056ac74eeca18ded0825a`。macOS 工作流通过，另外两条工作流发现了两个不同的测试输入问题：

1. PetPack contract 的 Windows job 发现 Python `ZipInfo.filename` 会在 Windows 上先把反斜线改成正斜线，原测试没有实际写入恶意原始路径。`1937a03` 改用 `orig_filename` 校验并固定测试归档原始条目名。
2. Windows x64 job 发现 .NET `ZipArchive` 在 Windows 上不会因为写入 Unix mode 就自动设置 Unix 创建主机，symlink 测试因此变成普通文件。`f6b855f` 直接固定测试归档中央目录的创建主机，不放宽生产验证器。

修复与文档锚点推送后，远端 `main` 独立回读为 `97485a0c5ccc557748773738dac5e009cf221753`。三条工作流均在该 SHA 上通过：

| 工作流 | 运行 | 结论 |
|---|---|---|
| PetPack contract | `32596460282` | macOS 14 与 Windows 2025 的测试、夹具校验和确定性重建全部通过 |
| macOS Apple Silicon build | `32596460260` | Swift 测试、零素材 arm64 App 构建和上传全部通过 |
| Windows x64 build | `32596460309` | 依赖还原、20 项测试、WPF 构建、self-contained ZIP、x64 PE、零素材和上传全部通过 |

远端代码开发产物保留 7 天：

- `PetsGraph-0.7.0-dev-macOS-arm64`，上传大小 1,806,750 bytes，过期时间 2026-08-30 04:22:45（Asia/Shanghai）。
- `PetsGraph-v0.7.0-dev-Windows-x64`，上传大小 77,508,772 bytes，过期时间 2026-08-30 04:23:02（Asia/Shanghai）。

最终审查修复继续按切片推送，并产生以下成功运行：

| 工作流 | 运行 | 提交或标签 | 结论 |
|---|---|---|---|
| PetPack contract | `32600312955` | `8b8f902` | macOS 与 Windows 上的 33 项严格容器与语义回归通过 |
| macOS Apple Silicon build | `32600330583` | `b417a6b` | 21 项 Swift 测试、零素材 arm64 App 构建和上传通过 |
| Windows x64 build | `32600330777` | `b417a6b` | 38 项 MSTest、WPF、self-contained x64 ZIP、零素材和上传通过 |
| Verify macOS release DMG | `32600568305` | `v0.6.0` | 从不可移动标签运行历史 Swift 基线，下载公开双附件，核对摘要并挂载验证 DMG 成功 |
| Verify Windows release ZIP | `32600570363` | `v0.6.0` | 使用 `contents: read` 下载公开 ZIP，核对摘要、AMD64 PE、双包与运行时完整性成功 |

CI 全绿仍不能替代真实双平台 GUI 与用户视觉批准门禁。
